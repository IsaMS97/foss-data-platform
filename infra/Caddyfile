# Dockhand service
dockhand.platform.smith-data.de {
    reverse_proxy dockhand:3000
    tls internal
}

# Simple website - HTTP only by binding to port 80 explicitly
website.platform.smith-data.de:80 {
    reverse_proxy http://simple-website:80
}